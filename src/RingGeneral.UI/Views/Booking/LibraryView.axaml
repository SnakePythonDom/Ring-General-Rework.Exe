<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:RingGeneral.UI.ViewModels.Booking"
             mc:Ignorable="d" d:DesignWidth="1200" d:DesignHeight="800"
             x:Class="RingGeneral.UI.Views.Booking.LibraryView"
             x:DataType="vm:LibraryViewModel">

    <Design.DataContext>
        <vm:LibraryViewModel />
    </Design.DataContext>

    <ScrollViewer>
        <StackPanel Margin="30" Spacing="25">
            <!-- En-tête -->
            <StackPanel Spacing="8">
                <TextBlock Text="📚 BIBLIOTHÈQUE DE TEMPLATES" FontSize="28" FontWeight="Bold" Foreground="#e0e0e0"/>
                <TextBlock Text="Gérez vos templates réutilisables pour le booking" FontSize="14" Foreground="#888888"/>
            </StackPanel>

            <!-- Statistiques -->
            <Grid ColumnDefinitions="*,*,*">
                <Border Grid.Column="0" Background="#1e293b" Padding="20" CornerRadius="8" Margin="0,0,6,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="📋 TEMPLATES" FontSize="12" Foreground="#64748b" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding TemplateCount}" FontSize="32" FontWeight="Bold" Foreground="#3b82f6"/>
                        <TextBlock Text="Disponibles" FontSize="11" Foreground="#94a3b8"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#065f46" Padding="20" CornerRadius="8" Margin="6,0,6,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="🤼 MATCHES" FontSize="12" Foreground="#d1fae5" FontWeight="SemiBold"/>
                        <TextBlock Text="3" FontSize="32" FontWeight="Bold" Foreground="#34d399"/>
                        <TextBlock Text="Templates" FontSize="11" Foreground="#a7f3d0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Background="#7c2d12" Padding="20" CornerRadius="8" Margin="6,0,0,0">
                    <StackPanel Spacing="8">
                        <TextBlock Text="🎤 PROMOS" FontSize="12" Foreground="#fed7aa" FontWeight="SemiBold"/>
                        <TextBlock Text="2" FontSize="32" FontWeight="Bold" Foreground="#fb923c"/>
                        <TextBlock Text="Templates" FontSize="11" Foreground="#fdba74"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Barre de recherche et filtres -->
            <Grid ColumnDefinitions="*,Auto">
                <Border Grid.Column="0" Background="#1e293b" Padding="15" CornerRadius="8" Margin="0,0,8,0">
                    <TextBox Watermark="🔍 Rechercher un template..."
                             Text="{Binding SearchText}"
                             Background="#0f172a"
                             BorderBrush="#334155"
                             Foreground="#e0e0e0"
                             Padding="12,10"
                             CornerRadius="6"/>
                </Border>

                <ComboBox Grid.Column="1"
                        ItemsSource="{Binding Categories}"
                        SelectedItem="{Binding SelectedCategory}"
                        Background="#1e293b"
                        BorderBrush="#334155"
                        Foreground="#e0e0e0"
                        Padding="15,10"
                        MinWidth="150"/>
            </Grid>

            <!-- Actions rapides -->
            <Border Background="#1e293b" Padding="20" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="⚡ ACTIONS RAPIDES" FontSize="14" Foreground="#64748b" FontWeight="SemiBold"/>
                    <Grid ColumnDefinitions="*,*">
                        <Button Grid.Column="0"
                              Content="➕ Nouveau Template"
                              Command="{Binding CreateTemplateCommand}"
                              Padding="12,10"
                              Background="#3b82f6"
                              Foreground="White"
                              CornerRadius="6"
                              FontWeight="Bold"
                              Margin="0,0,5,0"
                              HorizontalAlignment="Stretch"
                              HorizontalContentAlignment="Center"/>
                        <Button Grid.Column="1"
                              Content="🔄 Actualiser"
                              Command="{Binding RefreshCommand}"
                              Padding="12,10"
                              Background="#8b5cf6"
                              Foreground="White"
                              CornerRadius="6"
                              FontWeight="Bold"
                              Margin="5,0,0,0"
                              HorizontalAlignment="Stretch"
                              HorizontalContentAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Liste des templates -->
            <Border Background="#1e293b" Padding="20" CornerRadius="8">
                <StackPanel Spacing="12">
                    <TextBlock Text="📋 TEMPLATES DISPONIBLES" FontSize="14" Foreground="#64748b" FontWeight="SemiBold"/>

                    <ItemsControl ItemsSource="{Binding Templates}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#0f172a" Padding="16" Margin="0,0,0,8" CornerRadius="6">
                                    <Grid ColumnDefinitions="2*,*,*,*,Auto">
                                        <!-- Nom et type -->
                                        <StackPanel Grid.Column="0" Spacing="4">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <TextBlock Text="{Binding Nom}" FontSize="14" Foreground="#e0e0e0" FontWeight="Bold"/>
                                                <Border Background="#3b82f6"
                                                      Padding="6,2"
                                                      CornerRadius="4"
                                                      IsVisible="{Binding EstMainEvent}">
                                                    <TextBlock Text="MAIN EVENT"
                                                             FontSize="9"
                                                             Foreground="White"
                                                             FontWeight="Bold"/>
                                                </Border>
                                            </StackPanel>
                                            <TextBlock Text="{Binding TypeSegmentLibelle}" FontSize="12" Foreground="#94a3b8"/>
                                        </StackPanel>

                                        <!-- Durée -->
                                        <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                                            <TextBlock Text="Durée" FontSize="10" Foreground="#64748b"/>
                                            <StackPanel Orientation="Horizontal" Spacing="4">
                                                <TextBlock Text="{Binding DureeMinutes}" FontSize="14" Foreground="#3b82f6" FontWeight="Bold"/>
                                                <TextBlock Text="min" FontSize="12" Foreground="#94a3b8" VerticalAlignment="Bottom"/>
                                            </StackPanel>
                                        </StackPanel>

                                        <!-- Intensité -->
                                        <StackPanel Grid.Column="2" Spacing="4" VerticalAlignment="Center">
                                            <TextBlock Text="Intensité" FontSize="10" Foreground="#64748b"/>
                                            <TextBlock Text="{Binding Intensite}" FontSize="14" Foreground="#f59e0b" FontWeight="Bold"/>
                                        </StackPanel>

                                        <!-- Match Type (si applicable) -->
                                        <StackPanel Grid.Column="3" Spacing="4" VerticalAlignment="Center" IsVisible="{Binding MatchTypeNom, Converter={x:Static StringConverters.IsNotNullOrEmpty}}">
                                            <TextBlock Text="Type de match" FontSize="10" Foreground="#64748b"/>
                                            <TextBlock Text="{Binding MatchTypeNom}" FontSize="12" Foreground="#94a3b8"/>
                                        </StackPanel>

                                        <!-- Actions -->
                                        <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                                            <Button Content="✏️"
                                                  Command="{Binding $parent[UserControl].DataContext.EditTemplateCommand}"
                                                  CommandParameter="{Binding}"
                                                  Padding="8,6"
                                                  Background="#3b82f6"
                                                  Foreground="White"
                                                  CornerRadius="4"
                                                  FontSize="14"
                                                  ToolTip.Tip="Éditer"/>
                                            <Button Content="📋"
                                                  Command="{Binding $parent[UserControl].DataContext.DuplicateTemplateCommand}"
                                                  CommandParameter="{Binding}"
                                                  Padding="8,6"
                                                  Background="#8b5cf6"
                                                  Foreground="White"
                                                  CornerRadius="4"
                                                  FontSize="14"
                                                  ToolTip.Tip="Dupliquer"/>
                                            <Button Content="🗑️"
                                                  Command="{Binding $parent[UserControl].DataContext.DeleteTemplateCommand}"
                                                  CommandParameter="{Binding}"
                                                  Padding="8,6"
                                                  Background="#ef4444"
                                                  Foreground="White"
                                                  CornerRadius="4"
                                                  FontSize="14"
                                                  ToolTip.Tip="Supprimer"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>

</UserControl>
