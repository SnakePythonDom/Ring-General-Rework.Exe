<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:RingGeneral.UI.ViewModels.Start"
             mc:Ignorable="d" d:DesignWidth="1600" d:DesignHeight="900"
             x:Class="RingGeneral.UI.Views.Start.CompanySelectorView"
             x:DataType="vm:CompanySelectorViewModel">

    <Design.DataContext>
        <vm:CompanySelectorViewModel />
    </Design.DataContext>

    <Grid Background="{StaticResource BackgroundPrimaryBrush}">
        <Grid RowDefinitions="Auto,*,Auto" Margin="60">

            <!-- En-tête -->
            <StackPanel Grid.Row="0" Spacing="12" Margin="0,0,0,40">
                <Button Content="← Retour"
                        Command="{Binding BackCommand}"
                        Padding="15,8"
                        Background="{StaticResource BorderMediumBrush}"
                        Foreground="{StaticResource TextPrimaryBrush}"
                        HorizontalAlignment="Left"/>

                <TextBlock Text="SÉLECTION DE COMPAGNIE" FontSize="48" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                <TextBlock Text="Choisissez la compagnie que vous souhaitez diriger" FontSize="18" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>

            <!-- Liste des compagnies -->
            <ScrollViewer Grid.Row="1">
                <StackPanel Spacing="15">

                    <!-- Bouton Créer Nouvelle Compagnie -->
                    <Border Background="{StaticResource BrandPrimaryBrush}"
                            CornerRadius="8"
                            Padding="30,20"
                            Cursor="Hand">
                        <Border.Styles>
                            <Style Selector="Border:pointerover">
                                <Setter Property="Background" Value="{StaticResource BrandPrimaryBrush}"/>
                            </Style>
                        </Border.Styles>

                        <Button Command="{Binding CreateNewCompanyCommand}"
                                Background="Transparent"
                                BorderThickness="0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Left"
                                Padding="0">
                            <StackPanel Orientation="Horizontal" Spacing="20">
                                <TextBlock Text="➕" FontSize="36" VerticalAlignment="Center"/>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="CRÉER UNE NOUVELLE COMPAGNIE" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                    <TextBlock Text="Démarrez avec une promotion personnalisée" FontSize="14" Foreground="{StaticResource BrandPrimaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Button>
                    </Border>

                    <!-- Liste des compagnies existantes -->
                    <ListBox ItemsSource="{Binding Companies}"
                             SelectedItem="{Binding SelectedCompany, Mode=TwoWay}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{StaticResource BackgroundSecondaryBrush}"
                                        CornerRadius="8"
                                        Padding="30,20"
                                        Margin="0,0,0,10"
                                        Cursor="Hand">
                                    <Border.Styles>
                                        <Style Selector="Border:pointerover">
                                            <Setter Property="Background" Value="{StaticResource BorderMediumBrush}"/>
                                        </Style>
                                    </Border.Styles>

                                    <Grid ColumnDefinitions="*,Auto">
                                        <!-- Info compagnie -->
                                        <StackPanel Grid.Column="0" Spacing="8">
                                            <TextBlock Text="{Binding Name}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>

                                            <StackPanel Orientation="Horizontal" Spacing="20">
                                                <StackPanel Orientation="Horizontal" Spacing="6">
                                                    <TextBlock Text="🌍" FontSize="14"/>
                                                    <TextBlock Text="{Binding Region}" FontSize="14" Foreground="{StaticResource TextMutedBrush}"/>
                                                </StackPanel>

                                                <StackPanel Orientation="Horizontal" Spacing="6">
                                                    <TextBlock Text="★" FontSize="14" Foreground="{StaticResource WarningBrush}"/>
                                                    <TextBlock Text="{Binding Prestige}" FontSize="14" Foreground="{StaticResource TextMutedBrush}"/>
                                                    <TextBlock Text="/100" FontSize="14" Foreground="{StaticResource TextSubtleBrush}"/>
                                                </StackPanel>

                                                <StackPanel Orientation="Horizontal" Spacing="6">
                                                    <TextBlock Text="💰" FontSize="14"/>
                                                    <TextBlock Text="{Binding TreasuryFormatted}" FontSize="14" Foreground="{StaticResource TextMutedBrush}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </StackPanel>

                                        <TextBlock Grid.Column="1"
                                                   Text="Sélectionner"
                                                   Foreground="{StaticResource AccentGreenBrush}"
                                                   FontWeight="Bold"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                </StackPanel>
            </ScrollViewer>

            <!-- Actions et aide en bas -->
            <StackPanel Grid.Row="2" Spacing="12" Margin="0,20,0,0">
                <Button Content="CONTINUER →"
                        Command="{Binding SelectCompanyCommand}"
                        Padding="25,12"
                        Background="{StaticResource AccentGreenBrush}"
                        Foreground="White"
                        FontWeight="Bold"
                        CornerRadius="6"
                        HorizontalAlignment="Left"/>

                <Border Background="{StaticResource BackgroundSecondaryBrush}" CornerRadius="8" Padding="20">
                    <StackPanel Spacing="8">
                        <TextBlock Text="💡 ASTUCE" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentBlueBrush}"/>
                        <TextBlock Text="Les compagnies avec un prestige élevé ont déjà une base de fans solide mais peuvent avoir moins de marge de progression. Les compagnies moins prestigieuses offrent un défi plus grand mais aussi plus de satisfactions."
                                  FontSize="13"
                                  Foreground="{StaticResource TextMutedBrush}"
                                  TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </StackPanel>

        </Grid>
    </Grid>

</UserControl>
